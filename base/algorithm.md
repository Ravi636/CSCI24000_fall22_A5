## Algorithom assigment 5

add all of the libary files for different math, string, stream, and file function

create input and output file objects

open input and output file and make sure they are opened

use a while loop and getline function to get every line from the text file and store it in a string

getline function needs to know where its getting the string from and where its storing the value

send the string into a stringstream

use a while loop and getline function to seprate every sting by a comma

getline function need to know where its getting the string from and where its storing the value and when is it stop reading the string

convert the strings into  integers and add up all the values

use getline function to get the next line in the file and store the value in a string

use a for loop to print the string upto the sum of the values of integers added up

close the input and output file objects


